language: android
script:
  - pwd
  - ls -l
  - cd $TRAVIS_BUILD_DIR
  - ls -l
  - cd $JAVA_HOME/bin
  - >-
    yes "1VykWMeF8w9A" | sudo keytool -genkey -v -keystore release_signature.jks
    -keyalg RSA -keysize 2048 -validity 10000 -alias my-alias
notifications:
  email: false
